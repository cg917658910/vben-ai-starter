# Create Detail Module Prompt

请基于当前仓库的 vben 风格新增一个详情模块。

要求：

- 优先沿用现有模块目录风格
- 如果详情是从列表进入，保持路由、返回逻辑和标题一致
- 如果详情适合弹窗或抽屉，优先复用 `useVbenDrawer` 或 `useVbenModal`
- 如果详情包含只读表单，优先复用现有 schema 思路，不另起一套字段映射模式
- 对于复杂区块，优先拆成子组件，但不要破坏当前目录结构的一致性

请先判断该模块更适合：

- 独立详情页
- Drawer 详情
- Modal 详情

并说明理由后再实现。
