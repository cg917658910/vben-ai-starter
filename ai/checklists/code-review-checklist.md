# Code Review Checklist

- 结构是否符合项目既有模式，而不是作者个人习惯
- 是否有本可复用但被重复实现的表单或表格逻辑
- `list.vue` 是否过于臃肿，本该移到 `data.ts` 的内容是否拆出
- `form.vue` 是否同时承担过多无关逻辑
- API 和类型是否统一维护在 `src/api/*`
- 是否存在未清理的前端临时字段
- 路由、权限、菜单字段是否一致
- 状态切换、删除、保存是否存在漏刷、漏锁、漏报错
- 是否给出了最基本的验证方式
